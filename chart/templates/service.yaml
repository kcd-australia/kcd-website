apiVersion: v1
kind: Service
metadata:
  name: {{ include "kcd.fullname" . }}
  labels:
    {{- include "kcd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kcd.selectorLabels" . | nindent 4 }}
